---
title: "Validation of spatial methods in `cityClimateHealth`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Validation of spatial methods in `cityClimateHealth`}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(cityClimateHealth)
```

need a validation rmd of the data with larger population sizes and low variance and maybe just one lag so its easy to see

or maybe just one populations segment
