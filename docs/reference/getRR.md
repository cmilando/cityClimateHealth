# getRR base class

getRR base class

## Usage

``` r
getRR(x, ...)
```

## Arguments

- ...:

## Examples

``` r
if (FALSE) { # \dontrun{
# after running a condPois model:
result <- condPois_2stage(exposure_matrix, outcomes_tbl)
getRR(result)
} # }
```
