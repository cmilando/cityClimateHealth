# Make crossbasis object

Make crossbasis object

## Usage

``` r
make_crossbasis(
  exposure_matrix,
  outcomes_tbl,
  unique_geos,
  maxlag,
  argvar,
  arglag,
  strata_min,
  min_n,
  verbose
)
```

## Arguments

- verbose:
